* More default formatters (currently has just enough formatters to pass tests)
* Test the java-specific parts (lookup names inside a bean)

DONE:
* Ant build script? DONE. Thanks to Hiroaki Nakamura.

